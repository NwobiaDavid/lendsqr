import React from 'react';

const UserPage: React.FC = () => {
  return (
    <div>
      <h1>User Page</h1>
      {/* Fetch and display the user records from the mock API */}
    </div>
  );
};

export default UserPage;
